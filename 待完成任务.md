# NewTso2Pmx 待完成任务

## 计划

- [x] 1. 贴图预览与替换
  - 在 TSO 管理页显示选中 TSO 的内嵌贴图列表。
  - 支持预览选中贴图。
  - 支持用 BMP/TGA/PNG 替换选中贴图并写回 TSO 数据。
  - 替换后刷新材质、预览和后续 TSO/PNG/PMX 导出数据。
- [x] 2. SubScript/Shader 文本导入导出
  - 支持导出选中 SubScript 的原始文本。
  - 支持导出全部 SubScript。
  - 支持从文本导入并替换选中 SubScript。
  - 导入后重新生成 Shader 参数并同步材质列表。
- [x] 3. 多 Figure 会话
  - 支持从 TDCG PNG 追加 Figure 到当前会话。
  - 支持切换当前 Figure。
  - 支持删除当前 Figure，并保持至少一个 Figure。
  - 更新导出逻辑，使当前 Figure 选择明确。
- [x] 4. TDCG PNG 完整保存增强
  - [x] 4.1 使用当前预览生成真实缩略图。
  - [x] 4.2 尽量保留或重建旧 PNG 的关键 chunk。
  - [x] 4.3 明确支持角色 PNG 与 pose-only PNG 的差异。
- [x] 5. Tmo2Vpd / VPD 导出评估与实现
  - [x] 使用现有 `Girl2Miku_Default` 骨骼表重建 VPD 导出映射，不再依赖旧项目缺失的 `girl2miku_tmo2vpd.txt`。
  - [x] 移植 `TransBoneRotation.SaveVpd` 所需姿势差分、PMD 初始姿势差分和 VPD 文本写出逻辑。
  - [x] 增加当前 Figure 到 VPD 的导出入口。
- [ ] 6. Vmd2Tmo 评估与实现（进行中）
  - [x] 确认旧 `Vmd2Tmo` 依赖缺失的 `NyMmd` / `NyMmdUtils`，当前旧源码与 Release 目录均未提供这些依赖。
  - [x] 实现基础 VMD 解析，读取骨骼帧和表情帧。
  - [x] 使用 `Girl2Miku_Default/bonePosition.txt` 生成 Miku 骨骼到 TDCG 骨骼的基础映射。
  - [x] 增加“导入 VMD 为 TMO”入口，将基础转换结果应用到当前 Figure，后续可用现有 TMO/PNG 导出保存。
  - [ ] 补齐 PMD 参考骨架选择与旧 NyMmd 播放器等价的 IK / 约束处理。
  - [x] 补齐 VMD 贝塞尔插值曲线，替代当前基础线性位置插值和球面旋转插值。
  - [ ] 用真实 VMD/PMD 样例做视觉和动作对齐校验。
- [ ] 7. Viewer 渲染等价增强
  - [x] 预览渲染使用 TSO Shader 的 ColorTex 贴图。
  - [x] 预览渲染使用 TSO Shader 的 ShadeTex toon ramp。
  - [x] 预览渲染使用 TSO Shader 的 PenColor/Thickness 描边。
  - [ ] 逐步补齐旧 Direct3D Viewer 的阴影等效果。
  - 保持跨平台 OpenGL 渲染路径。
- [x] 8. 配置和会话状态持久化
  - [x] 保存常用输出选项。
  - [x] 保存 UI 选择状态和最近目录。
  - [x] 保存 TSO 分类和当前编辑会话。

## 进度记录

- 2026-06-09：创建待完成任务计划。
- 2026-06-09：开始实现任务 1：贴图预览与替换。
- 2026-06-09：完成任务 1：TSO 管理页已支持贴图列表、贴图预览，以及用 BMP/TGA/PNG 替换选中贴图；`dotnet build .\NewTso2Pmx.App\NewTso2Pmx.App.csproj -v minimal` 通过，仍有旧 Core 既有警告。
- 2026-06-09：开始实现任务 2：SubScript/Shader 文本导入导出。
- 2026-06-09：完成任务 2：TSO 管理页已支持导出选中 SubScript、导出全部 SubScript、导入文本替换选中 SubScript；增量构建 0 警告 0 错误。
- 2026-06-09：开始评估任务 3：多 Figure 会话。
- 2026-06-09：完成任务 3：App 已持有内存 Figure 会话，支持切换当前 Figure、从 TDCG PNG 追加 Figure、删除当前 Figure；导出 PNG/TMO/PMX 明确使用当前 Figure。完整构建 0 错误，仍有旧 Core 既有警告。
- 2026-06-09：开始任务 4：TDCG PNG 完整保存增强，优先实现真实预览图写入。
- 2026-06-09：完成任务 4.1：导出当前 Figure TDCG PNG 时会优先写入当前 OpenGL 预览截图作为 PNG 图像内容，捕获失败时回退到原 1px 透明图。完整构建 0 错误，仍有旧 Core 既有警告。
- 2026-06-09：完成任务 4.2：导出 TDCG PNG 时会从 PNG 源文件保留安全的 ancillary PNG chunk；TDCG `taOb` 模型数据仍由当前文档重建，避免混入旧模型数据。完整构建 0 错误，仍有旧 Core 既有警告。
- 2026-06-09：完成任务 4.3：角色 PNG 与 pose-only 姿势 PNG 已拆分；角色 PNG 导出当前 Figure 的 SCNE/FIGU/FTSO，姿势 PNG 只写 POSE/FTMO/LGTA，可导入到当前 Figure。完整构建 0 错误，仍有旧 Core 既有警告。
- 2026-06-09：开始任务 5：Tmo2Vpd / VPD 导出评估与实现，优先核对旧项目映射表和 `TransBoneRotation.SaveVpd` 依赖。
- 2026-06-09：完成任务 5：新增 Core `VpdExporter`，模型概览页增加“导出 VPD”；导出使用当前 Figure 姿势和 `Girl2Miku_Default` 骨骼表生成 Shift-JIS VPD。已用 `标准身体.tdcgsav.png` 做 VPD 烟测，生成 132 个骨骼条目；`dotnet build .\NewTso2Pmx.App\NewTso2Pmx.App.csproj -v minimal` 通过，0 警告 0 错误。
- 2026-06-09：开始任务 6：Vmd2Tmo 评估与实现，优先核对旧 `Vmd2Tmo` 的 VMD/PMD 解析、骨骼对应表和动作转换依赖。
- 2026-06-09：完成任务 6 的基础切片：新增 `VmdFile` 与 `VmdTmoConverter`，支持读取 VMD 骨骼/表情帧并转换为当前 Figure 的 TMO；模型概览页增加“导入 VMD 为 TMO”。旧源码缺失 `NyMmd` / `NyMmdUtils`，因此 PMD/IK 等价播放和 VMD 贝塞尔插值仍保留为任务 6 未完成子项。完整构建 0 错误，仍有旧 Core 既有警告。
- 2026-06-09：完成任务 6 的 VMD 贝塞尔插值子项：新增 `VmdInterpolation`，VMD 骨骼位置 X/Y/Z 与旋转通道会按关键帧自带贝塞尔曲线采样，非法插值数据回退线性；已做插值冒烟验证，`dotnet build .\NewTso2Pmx.App\NewTso2Pmx.App.csproj -v minimal` 通过，0 警告 0 错误。
- 2026-06-09：完成任务 8 的配置持久化基础切片：新增 App JSON 设置，保存/恢复 PMX 输出选项、骨骼/材质/物理选项、模板选择、网格分组模式，以及文件选择器最近输入/输出目录；`dotnet build .\NewTso2Pmx.App\NewTso2Pmx.App.csproj -v minimal` 通过，0 警告 0 错误。
- 2026-06-09：完成任务 8 的会话状态切片：设置文件会记录最后编辑来源、Figure 索引、选中 TSO、TSO 分类和子网格裁剪选择；重新加载同一来源时恢复这些轻量会话状态，不在启动时隐式读取大模型。`dotnet build .\NewTso2Pmx.App\NewTso2Pmx.App.csproj -v minimal` 通过，0 警告 0 错误。
- 2026-06-09：完成任务 7 的 ColorTex 预览渲染切片：Preview 数据携带 UV、draw batch 和 TSO 贴图像素，OpenGL 渲染器按材质批次上传并绑定 ColorTex；无贴图材质仍回退调色板颜色。`dotnet build .\NewTso2Pmx.App\NewTso2Pmx.App.csproj -v minimal` 通过，0 错误，完整 Core 构建仍有既有 warning。
- 2026-06-09：完成任务 7 的 ShadeTex/toon ramp 预览渲染切片：Preview draw batch 携带 ShadeTex 与 LightDir，OpenGL 预览按材质批次绑定 ColorTex/ShadeTex；无 ShadeTex 材质仍回退 Lambert 光照。`dotnet build .\NewTso2Pmx.App\NewTso2Pmx.App.csproj -v minimal` 通过，0 错误，完整 Core 构建仍有 182 个既有 warning。
- 2026-06-09：完成任务 7 的描边预览渲染切片：按 TSO Shader 的 `PenColor`/`Thickness` 和非 `InkOff` 的 Shadow technique 生成 OpenGL inverted-hull 描边 pass；特殊透明/加算 technique 暂不强制套描边。`dotnet build .\NewTso2Pmx.App\NewTso2Pmx.App.csproj -v minimal` 通过，0 错误，完整 Core 构建仍有 182 个既有 warning。
